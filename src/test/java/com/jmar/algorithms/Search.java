package com.jmar.algorithms;

import org.junit.jupiter.api.Test;

public class Search {

    @Test
    public void binarySearchTest() {

    }

    /**
     * Binary search only works with a sorted list.
     *
     * @param integers
     * @param val
     * @return
     */
    public int binarySearch(int [] integers, int val) {
         boolean done = false;
         int lowerIndex = 0;
         int upperIndex = integers.length - 1;
         while(!done) {

         }
         return -1;
    }

}
